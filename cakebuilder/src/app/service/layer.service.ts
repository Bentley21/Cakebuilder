import {Injectable} from '@angular/core';
import {layer} from '../model/layer';

Injectable({
    providedIn: 'root'
})
export class LayerService {

    constructor() { }
    getLayer() : layer[]
        return this.layer;

}